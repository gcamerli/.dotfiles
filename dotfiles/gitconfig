[user]
	name = Gius. Camerlingo

[core]
	editor = vim
	excludesfile = .gitignore

[diff]
	tool = vimdiff

[color]
	ui = auto

[color "branch"]
	current = yellow reverse
	local = yellow
	remote = green

[color "diff"]
	meta = yellow bold
	frag = magenta bold
	old = red bold
	new = green bold

[color "status"]
	added = green
	changed = red
	untracked = blue

[push]
	default = current
